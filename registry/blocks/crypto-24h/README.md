# Crypto + 24h change

Spot price in USD plus the 24-hour change percent from CoinGecko's keyless
endpoint (one request per refresh, well inside their free rate limit).
A richer take on the original `crypto-price` block; use that one if you
only want the number. Suggested size: 5x3, or 5x4 to see the change line.
