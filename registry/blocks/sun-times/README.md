# Sunrise & sunset

Today's sunrise and sunset for your location, already converted to your
timezone (pass an IANA name like `Europe/Berlin` in the `tz` parameter).
Data from sunrise-sunset.org, which asks for attribution, hereby given.
One text line, so 7x2 is enough.
